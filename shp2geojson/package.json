{
  "name": "shp2geojson",
  "version": "0.0.1",
  "description": "Convert shapefile to geoJSON via a web browser without Server-Side code. This conversion will unzip your file and reproject the data with correct encoding in JavaScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "prepublishOnly": "npm run build"
  },
  "author": "gipong",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.3",
    "@babel/preset-react": "^7.24.7",
    "babel-cli": "^6.26.0",
    "babel-loader": "^9.1.3",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "jszip-utils": "^0.1.0",
    "proj4": "^2.11.0"
  }
}
